name: CI

# The suite is the whole build: main.js is hand-written CommonJS with no bundler
# and no runtime dependencies, and test/core.test.js stubs the host modules so it
# loads in plain Node. A green run means the committed main.js behaves as the
# README documents.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - name: Install
        run: npm ci
      - name: Check the entry file parses
        run: node --check main.js
      - name: Run the assertions
        run: npm run build
